# Phase prompts

The brief each phase was built from, committed verbatim, plus the decisions
that answered its open questions. A summary says what was built; these say
what was asked for and what was ruled out, which is the part that is hardest
to reconstruct later.

Read the matching `docs/phases/phase-N.md` alongside: the prompt is the
request, the summary is the outcome and the deviations.
